import json

from owprofile.blizz_interface import get_user_page
from owprofile.parsing import parse_stats, parse_hero_stats, parse_hero_playtime, parse_achievement_data

"""
def get_user_page(page_text: str) -> etree._Element:

Pass the contents of the HTML file to this file and pass the output to the data parsing functions

Everything else is pretty self-explanatory
"""

